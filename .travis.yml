language: python
cache: pip
python:
    - "3.6"
    - "3.7"

install:
    - pip install .[test,precommit]

# run tests
script:
    - which prospector
    - pre-commit run --all-files
    - cd tests
    - pytest
